

// const variable 
// Also block-scoped.
// Cannot be redeclared or updated.
// Must be initialized when declared.
const user={ name: 'ani'};
user.name='karthi';
console.log(user.name);

//for objects and arrays declared with (const) we can modify their content ,
// but not reassing their variable